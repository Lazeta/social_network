let initialState = {
    music: {},
}

const musicReducer = (state = initialState, action) => {
    switch(action.type) {
        default: {
            return state;
        }
    }
}

export default musicReducer;